<?php
// Application middleware

// use App\Middleware\LanguageSetter;
// use Noodlehaus\Config;

$container = $app->getContainer();

// $app->add(new LanguageSetter($container->get('request'), $container->get(Config::class)));
